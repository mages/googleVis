Version 0.2.0
================
Changes

   o The package has been renamed from GoogleMotionChart to googleVis to reflect a new more flexible implementation.
     As a result more function for the Google Visualisation API are available to the user

NEW FEATURES

   o New functions:
     - gvisMotionChart to generate motion charts
     - gvisGeoMap to generate geographical maps
     - gvisTreeMap to generate tree maps
     - gvisTable to generate table output
     - print.gvis: generic function to print 'gvis' objects
     - plot.gvis: generic function to display 'gvis' chart in your local browser via the R.rsp package. 

USER-VISIBLE CHANGES
   o New interfaces, all visualisation functions start with 'gvis'.
   o Output is now of class 'gvis' with generic print and plot functions.

Version 0.1.4
================
Changes

   o Packages uses now the RJSONIO package from Omegahat to transform a data.frame into a datatable

Version 0.1.3
================
NEW FEATURES

   o More detail motion chart configuration option settings are possible.

USER-VISIBLE CHANGES
   o options have to set via a list. Arguments height and width have to set via the options list. 
     However, further configuration options can now be set.
   o Update demo PerformanceAnalyticsMotionChart   


Version 0.1.2
================

First public version.
